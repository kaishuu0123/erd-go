package main_test

import (
	"testing"
)

func TestErd_unquote(t *testing.T) {
	// e := &Erd{}
	// fmt.Println(e)
	// value := e.unquote("\"test\"")
	// if value != "test" {
	// 	t.Errorf("got: %v\nwant: %v", value, "test")
	// }
}
